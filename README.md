# namples.github.io

개인 사이트. 주소는 **https://namples.github.io**

---

## 언어 3개 (한국어 / English / 日本語)

주소가 언어별로 나뉩니다. 오른쪽 위 `KO / EN / JA` 로 오갑니다.

| 언어 | 주소 |
|---|---|
| 한국어 (기본) | `namples.github.io/` |
| English | `namples.github.io/en/` |
| 日本語 | `namples.github.io/ja/` |

**메뉴·버튼 같은 화면 글자는 이미 세 언어로 다 번역돼 있습니다.** 고치려면 `src/i18n/ui.ts` 한 파일만 보면 됩니다.
홈 화면 소개 문구는 `src/site.ts`, 게임 설명은 `src/games.ts` 에 세 언어가 나란히 들어 있습니다.

### 글/작업물을 번역하고 싶을 때

파일 이름 뒤에 `.en` 또는 `.ja` 만 붙여서 하나 더 만들면 됩니다.

```
src/content/blog/봄날.md      ← 한국어 (이게 있어야 함)
src/content/blog/봄날.en.md   ← 영어판
src/content/blog/봄날.ja.md   ← 일본어판
```

- 안 만들어도 됩니다. 없으면 **한국어판이 그대로 나오고**, "이 글은 아직 한국어판만 있습니다" 라고 알려줍니다.
- 주소는 셋 다 같습니다. 언어 버튼만 누르면 오갑니다.

## 글씨체 바꾸기

사이트 맨 아래에 **손글씨 / 명조 / 고딕 / 타자기** 네 가지가 있습니다.
보는 사람이 직접 고르고, 그 사람 브라우저에 기억됩니다.
기본값이나 목록을 바꾸려면 `src/site.ts` 의 `fonts` 와 `src/styles/global.css` 의 `[data-font=...]` 부분을 보세요.

## 작업물 관리 화면 (내 전용)

`관리.cmd` 더블클릭 → 브라우저에 관리 화면이 열립니다.

- 작업물이 **썸네일 격자**로 쭉 보입니다
- 카드 오른쪽 위 **✕ 를 누르면 삭제** (완전히 지우는 게 아니라 `_휴지통/` 폴더로 옮겨두니 되살릴 수 있습니다)
- 제목·설명·분류·날짜를 그 자리에서 고칠 수 있습니다 (칸 밖을 클릭하면 자동 저장)
- **홈에 띄우기** 체크 → 홈 화면 대표 작업으로
- **숨기기** 체크 → 내 컴퓨터에선 보이지만 인터넷에는 안 올라감
- 그림 파일을 **끌어다 놓으면** 작업물이 바로 만들어집니다
- 오른쪽 위 **인터넷에 올리기** 버튼 → 여기서 바로 배포까지

> 이 화면은 내 컴퓨터에서만(127.0.0.1) 열립니다. 인터넷에는 절대 안 뜨고, 비밀번호도 필요 없습니다.

## X(트위터) 작업물 가져오기

`X가져오기.cmd` 더블클릭.

먼저 X에서 데이터를 받아와야 합니다:
**X 설정 → 계정 → 데이터 아카이브 다운로드** 를 신청하면 X가 준비해줍니다 (보통 하루쯤 걸립니다).
받은 zip 파일의 압축을 풀고, 그 폴더를 `X가져오기.cmd` 창에 끌어다 놓으면 끝입니다.

- 그림/영상이 붙은 글만 가져옵니다 (리트윗·답글은 건너뜀)
- 가져온 것은 **전부 "숨김" 상태**로 들어갑니다 → `관리.cmd` 에서 보여줄 것만 고르고 나머지는 ✕
- 여러 번 실행해도 이미 가져온 건 건너뛰니 안전합니다

## 매일 쓰는 것 딱 3개

### 1. 사이트 미리보기 (내 컴퓨터에서만 보임)

`미리보기.cmd` 를 더블클릭 → 브라우저에서 http://localhost:4321 열기.
파일을 고치면 화면이 알아서 바뀝니다. 끌 때는 검은 창에서 `Ctrl + C`.

### 2. 글 쓰기

`src/content/blog/` 폴더에 `.md` 파일을 하나 만들면 끝입니다.

```
---
title: 글 제목
date: 2026-08-05
summary: 목록에 한 줄로 뜨는 설명
tags: [태그1, 태그2]
---

여기부터 본문. 그냥 쓰면 됩니다.

## 소제목은 이렇게

**굵게**, *기울임*, [링크](https://example.com)

![그림 설명](/images/파일이름.png)
```

- 파일 이름이 주소가 됩니다. `첫글.md` → `/blog/첫글/`
- 아직 공개하기 싫으면 맨 위에 `draft: true` 를 넣으세요. 내 컴퓨터에선 보이고 인터넷에는 안 올라갑니다.
- 글 속 그림은 `public/images/` 폴더에 넣고 `/images/파일이름.png` 로 부르면 됩니다.

### 3. 작업물 올리기

`src/content/works/` 폴더에 `.md` 파일을 하나 만듭니다.

```
---
title: 작업 제목
date: 2026-08-05
category: 애니메이션      # 애니메이션 / 일러스트 / 게임 … 같은 이름끼리 묶입니다
summary: 한 줄 설명
cover: ../../assets/works/그림.png    # 그림은 src/assets/works/ 에 넣기
video: https://www.youtube.com/watch?v=xxxx   # 유튜브면 이 줄만 있어도 됨
featured: true            # 홈 화면에 띄우고 싶을 때
---

작업 설명을 자유롭게.
```

`cover` 에 쓴 그림은 사이트가 알아서 줄이고 최적화하니까 원본 크기 그대로 넣어도 됩니다.

---

## 인터넷에 올리기 (배포)

`배포.cmd` 를 더블클릭하면 끝입니다.
1~2분 뒤 https://namples.github.io 에 반영됩니다.

> 무슨 일이 일어나냐면: 바뀐 파일이 GitHub에 올라가고, GitHub이 알아서 사이트를 만들어 인터넷에 띄웁니다.
> 내 컴퓨터가 꺼져 있어도 사이트는 계속 살아 있습니다.

---

## 게임 추가하기

1. `public/games/` 안에 폴더를 하나 만들고 `index.html` 을 넣습니다.
   - 직접 만든 HTML 게임도 되고,
   - **유니티에서 WebGL로 빌드한 결과물 폴더**를 통째로 넣어도 됩니다.
2. `src/games.ts` 파일에 한 줄 추가합니다.

```ts
{
  slug: '폴더이름',
  title: '게임 이름',
  summary: '한 줄 소개',
  controls: '조작법',
},
```

3. `배포.cmd` 실행.

`star-catch` 폴더가 예시로 들어 있으니 흉내 내면 됩니다.

---

## 이름·소개·링크 바꾸기

`src/site.ts` 파일 하나만 고치면 사이트 전체에 반영됩니다.
(이름, 홈 화면 문구, 인스타·유튜브 링크 등)

---

## 폴더가 뭐가 뭔지

| 폴더 | 뭐가 들었나 |
|---|---|
| `src/content/blog/` | 블로그 글 (.md) |
| `src/content/works/` | 작업물 설명 (.md) |
| `src/assets/works/` | 작업물 그림 |
| `public/images/` | 글 속에 넣는 그림 |
| `public/games/` | 게임 |
| `public/works-media/` | X에서 가져온 영상 |
| `_휴지통/` | ✕로 지운 작업물 (인터넷에 안 올라감) |
| `src/site.ts` | 이름·소개·링크 |
| `src/games.ts` | 게임 목록 |
| `scripts/` | 관리 화면·X 가져오기 프로그램 |
| `src/pages/`, `src/styles/` | 사이트 뼈대와 디자인 (건드릴 일 거의 없음) |

---

만든 도구: [Astro](https://astro.build) · 호스팅: GitHub Pages (무료)
